---
name: Trilho de recuperação "copiou o código PIX"
description: Régua única com dois trilhos exclusivos (genérico 15min vs copiou 20min/2h), gate por ContentSid aprovado em system_config.wa_copiou_templates
type: feature
---

- `checkout_sessions` ganhou `pix_copied_at`, `wa_copiou_20min_sent_at`, `wa_copiou_2h_sent_at`.
- `criar-pix-recorrente-woovi` devolve `checkoutSessionId`; o CheckoutV2 chama `mark-pix-copied` ao copiar o copia-e-cola (evento de funil é anônimo e não serve pra isso).
- Régua ÚNICA no `recover-abandoned-checkout-whatsapp`: trilho "copiou" roda ANTES do genérico; quem copiou fica fora do genérico de 15min (`pix_copied_at IS NULL` no seletor) e, ao receber a 1ª msg do trilho, o 15min é fechado com `skipped: trilho_copiou`. Os dois trilhos convergem no estágio de 24h.
- Gate: trilho fica DESLIGADO enquanto `system_config.wa_copiou_templates.m1` não tiver ContentSid aprovado pela Meta. **Regra do Eduardo: NUNCA liberar o trilho sem template novo criado, configurado e aprovado pela Meta antes.**
- **LIGADO em 31/08/2026** com os dois templates aprovados (quick reply, pt_BR, categoria Marketing, subconta de recuperação):
  - **m1 (15 min)** `copy_of_recuperacao_pix_copiaecola_15min` = `HX438035e6d8892b4463e99b6abfaad832` — botões: Ficou uma dúvida / Vou pagar agora / Tive um erro. `minAgeMinutes` do estágio virou 15 (nome interno segue `copiou_20min` por causa das colunas).
  - **m2 (2h)** `copy_of_recuperacao_pix_copiaecola_2hs` = `HX8a208cd323ef2b99c92790caa0118b25` — botões: Gerar novo código / Já paguei / Tenho uma dúvida.
- **Motivo da 1ª recusa da Meta** (registro pra não repetir): "Variables can't be at the start or end of the template" (code 100 / subCode 2388299). Corpo começando com `{{1}}` é rejeitado — usar "Olá {{1}}, ...". A versão recusada também era `twilio/text` (sem botões); quick reply é obrigatório pra abrir a janela de 24h.
- **Cliques de botão** chegam no `webhook-twilio-recovery` como texto (`ButtonText`) e são resolvidos deterministicamente em `recovery-agent/pix-buttons.ts`, ANTES do LLM: "Gerar novo código"/"Tive um erro" → guarda ao vivo Woovi + reusa `qr_payload` válido ou gera novo via `criar-pix-recorrente-woovi` (CPF vem de `woovi_subscriptions`), com trava de 1 código por hora por telefone; "Já paguei" → confere ao vivo e confirma acesso ou pede comprovante; dúvidas/"Vou pagar agora" → seguem pro agente conversacional com bloco `CONTEXTO DECISIVO` (copiou e travou, não recomeçar a venda).
- Validação sem envio: `POST recover-abandoned-checkout-whatsapp {"dryRun": true}` lista candidatos, trilho e motivo de skip sem enviar nada.
- Guardas compartilhadas intactas: cliente ativo, já pago, guarda Woovi ao vivo, cap de telefone 30d, cap de 3 falhas, quiet hours 22h-08h (só 2h/24h).
- **m3 (encontro avulso R$ 6,90)** `oferta_sessao_45min_unica` = `HX3e469c17798e5011a08b8ac157e6d81d` — aprovado pela Meta em 31/08/2026, Marketing, pt_BR, 1 variável (`{{1}}` = primeiro nome, fora das pontas), botões: Quero experimentar / Tenho uma dúvida. SID gravado em `system_config.wa_copiou_templates.m3` e em `whatsapp_templates` (`checkout_recovery_wa_taster`). O degrau só dispara quando `system_config.taster_enabled = true` (hoje **false**, aguardando teste ponta a ponta).
- **31/08/2026 — por que `pix_copied_at` vinha zerado**: o `mark-pix-copied` só era chamado quando `data.checkoutSessionId` voltava do provedor; quando não voltava, a cópia não era registrada (45 eventos `pix_copy` em 7d, 0 marcações). Correção: o checkout agora manda SEMPRE telefone+email, e o `mark-pix-copied` resolve a sessão PIX `created` mais recente (6h) por telefone/email quando não há id. Histórico reconstruído por correlação temporal entre `checkout_funnel_events.step='pix_copy'` e a sessão PIX criada até 20 min antes → 111 sessões marcadas (59 copiaram e não pagaram).
